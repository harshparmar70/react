import styles from './headers.module.css';

const Header = () => {
  return (
    <div className={styles.head}>
      <h3>Harsh</h3>
      <button>login</button>
    </div>
  );
};

export default Header;
